# ===========================================
# Configuration Bus Display
# ===========================================

# Port de l'application (défaut: 3000)
PORT=3000

# Environnement (development, production)
NODE_ENV=development

# ===========================================
# Configuration Redis (optionnel)
# ===========================================

# Activer Redis (true/false)
# Si false, utilise le cache Map local
REDIS_ENABLED=false

# URL de connexion Redis
REDIS_URL=redis://localhost:6379

# Préfixe des clés Redis
REDIS_PREFIX=bus-display:

# ===========================================
# Configuration Cache
# ===========================================

# Durée du cache en millisecondes (défaut: 45000 = 45s)
CACHE_TTL=45000

# ===========================================
# Configuration API
# ===========================================

# URL de base de l'API (pour environnement production)
NEXT_PUBLIC_API_URL=http://localhost:3000

# ===========================================
# Monitoring (optionnel)
# ===========================================

# Activer les métriques Prometheus
METRICS_ENABLED=true

# ===========================================
# Sécurité
# ===========================================

# Clé API pour accès admin (OBLIGATOIRE EN PRODUCTION)
# Générer avec: openssl rand -hex 32
ADMIN_API_KEY=change-me-in-production

# Clé API pour métriques (optionnel, utilise ADMIN_API_KEY par défaut)
METRICS_API_KEY=

# Liste des IPs autorisées (séparées par virgule, * pour toutes)
# Exemple: 192.168.1.1,10.0.0.1
ALLOWED_IPS=

# ===========================================
# CORS (optionnel)
# ===========================================

# Origines autorisées pour CORS (séparées par virgule)
# Exemple: https://example.com,https://app.example.com
ALLOWED_ORIGINS=
